# 🚀 快速开始 - UI 设计

## 5 分钟快速上手

### 方法 1: 使用可视化网页工具（推荐）

```bash
# 1. 打开设计工具
open design_tool.html

# 或者启动 HTTP 服务器
python -m http.server 8080
# 然后访问: http://localhost:8080/design_tool.html
```

**操作步骤：**

1. **选择颜色**
   - 左侧面板点击颜色选择器
   - 调整地板、墙壁、家具等颜色
   - 点击"应用颜色"

2. **设置区域**
   - 右侧面板点击"💼 工作桌区"
   - 在中间画布上点击想要的位置
   - 重复操作设置其他区域

3. **加载背景**（可选）
   - 点击"加载背景图"按钮
   - 选择你的 office_bg.png
   - 在背景上标记区域位置

4. **导出配置**
   - 点击"导出配置"按钮
   - 复制 JavaScript 代码
   - 粘贴到 `frontend/index.html`

---

### 方法 2: 使用 Python 命令行工具

```bash
# 运行设计工具
python design_office.py
```

**选择预设风格：**

输入 `1` - 现代简约风（浅色系）
```
地板: #e8e8e8  墙壁: #ffffff
科技: #4a90e2  植物: #6fcf97
```

输入 `2` - 科技未来风（暗色霓虹）
```
地板: #1a1a2e  墙壁: #0f3460
科技: #00d4ff  植物: #00ff88
```

输入 `3` - 温馨舒适风（暖色调）
```
地板: #ffeaa7  墙壁: #fab1a0
科技: #74b9ff  植物: #55efc4
```

输入 `4` - 豪华商务风（默认）
```
地板: #3a3a52  墙壁: #252538
科技: #4a9eff  植物: #4a9e5f
```

输入 `5` - 自定义颜色（完全自由）

**完成后：**
- 自动生成 `frontend/office_bg.png`
- 显示使用的颜色配置
- 可继续设计或退出

---

## 实战示例

### 示例 1: 创建"科技未来风"办公室

```bash
$ python design_office.py

🎨 HappyCapy Office UI - 办公室设计工具
============================================================
请选择设计风格：
1. 现代简约风 (Modern Minimalist)
2. 科技未来风 (Tech Futuristic)  👈 选这个
3. 温馨舒适风 (Cozy Comfortable)
4. 豪华商务风 (Luxury Business)
5. 自定义颜色 (Custom Colors)
0. 退出
============================================================

请选择 (0-5): 2

🎨 正在生成 科技未来风...
  ✓ 绘制地板...
  ✓ 添加窗户...
  ✓ 摆放家具...
  ✓ 添加装饰...

✅ 完成！
📁 保存位置: frontend/office_bg.png
🎨 风格: 科技未来风
📏 尺寸: 800 × 600 像素

🎨 使用的颜色配置：
  floor_base: #1a1a2e
  floor_alt: #16213e
  wall_color: #0f3460
  desk_wood: #533483
  tech_blue: #00d4ff
  plant_green: #00ff88
  chair_red: #ff0080
  sofa_blue: #7000ff

是否继续设计？(y/n): n

👋 感谢使用！
```

**查看效果：**
```bash
python backend/app.py
# 访问: http://localhost:18791
```

---

### 示例 2: 自定义颜色方案

```bash
$ python design_office.py

请选择 (0-5): 5

请输入自定义颜色（十六进制格式，如 #3a3a52）
按回车使用默认值

地板主色 (默认: #3a3a52): #2c3e50    👈 输入你的颜色
地板次色 (默认: #45455e): #34495e
墙壁颜色 (默认: #252538): #1a252f
桌子颜色 (默认: #8B6F47): #8e44ad
科技蓝色 (默认: #4a9eff): #3498db
植物绿色 (默认: #4a9e5f): #27ae60
椅子红色 (默认: #c44569): #e74c3c
沙发蓝色 (默认: #3867d6): #2980b9

✅ 完成！使用自定义配色方案
```

---

### 示例 3: 使用网页工具设置坐标

**步骤：**

1. **打开工具**
   ```bash
   open design_tool.html
   ```

2. **设置工作区坐标**
   - 点击右侧 "💼 工作桌区"
   - 在画布左下方点击（推荐位置：x=250, y=350）
   - 显示：`x: 250, y: 350`

3. **设置休息区坐标**
   - 点击右侧 "☕ 休息区"
   - 在画布右下方点击（推荐位置：x=700, y=500）
   - 显示：`x: 700, y: 500`

4. **设置其他区域**
   - 会议区：x=450, y=230
   - 服务器区：x=720, y=230

5. **导出配置**
   - 点击 "导出配置" 按钮
   - 弹出新窗口显示代码
   - 点击 "复制 JS 代码"

6. **应用到项目**
   ```javascript
   // 打开 frontend/index.html
   // 找到 areas = { ... }
   // 替换为导出的代码：

   areas = {
       workdesk: { x: 250, y: 350 },
       breakroom: { x: 700, y: 500 },
       meeting: { x: 450, y: 230 },
       server: { x: 720, y: 230 }
   };
   ```

---

## 对比效果

### 豪华商务风（默认）
- **特点**: 专业、大气、现代
- **适合**: 企业官网、商务展示
- **配色**: 深色系为主，金色点缀

### 科技未来风
- **特点**: 酷炫、霓虹、赛博朋克
- **适合**: 科技公司、创业团队
- **配色**: 深色背景，荧光配色

### 现代简约风
- **特点**: 清爽、明亮、简洁
- **适合**: 初创公司、设计工作室
- **配色**: 浅色系为主，轻松氛围

### 温馨舒适风
- **特点**: 温暖、友好、居家
- **适合**: 社区、教育、服务业
- **配色**: 暖色调，柔和过渡

---

## 常用配置模板

### 模板 1: 极简工作区

```javascript
// 只保留必要区域
areas = {
    workdesk: { x: 400, y: 300 },  // 居中工作
    breakroom: { x: 650, y: 450 }  // 右下休息
};

// 颜色：纯白极简
floor_base = '#ffffff'
wall_color = '#f5f5f5'
desk_wood = '#e0e0e0'
```

### 模板 2: 开放式办公

```javascript
// 多个工作区
areas = {
    desk1: { x: 200, y: 350 },
    desk2: { x: 400, y: 350 },
    desk3: { x: 600, y: 350 },
    lounge: { x: 400, y: 500 }
};
```

### 模板 3: 会议为中心

```javascript
// 突出会议区
areas = {
    meeting: { x: 400, y: 300 },   // 居中会议桌
    workdesk: { x: 200, y: 450 },  // 周边工作区
    breakroom: { x: 600, y: 450 }
};
```

---

## 调试技巧

### 1. 查看实际坐标

在浏览器中打开 UI，按 F12 打开控制台，输入：

```javascript
// 点击时显示坐标
game.input.on('pointerdown', (pointer) => {
    console.log(`x: ${Math.round(pointer.x)}, y: ${Math.round(pointer.y)}`);
});
```

### 2. 临时显示所有区域

```javascript
// 在控制台输入
Object.entries(areas).forEach(([name, pos]) => {
    console.log(`${name}: x=${pos.x}, y=${pos.y}`);
});
```

### 3. 快速预览颜色

```python
# 修改 create_isometric_office.py
# 在文件顶部添加测试颜色
TEST_MODE = True
if TEST_MODE:
    floor_base = '#your_color'
    print(f"Testing color: {floor_base}")
```

---

## 下一步

完成基础设计后，你可以：

1. **添加动画效果** - 参考 [Phaser Tween 文档](https://photonstorm.github.io/phaser3-docs/Phaser.Tweens.Tween.html)
2. **创建自定义角色** - 使用 [Piskel](https://www.piskelapp.com/)
3. **添加音效** - 使用 Phaser 的音频系统
4. **移动端优化** - 调整触摸事件响应
5. **添加更多状态** - 扩展状态系统

---

## 获取帮助

- **完整设计指南**: `UI_DESIGN_GUIDE.md`
- **项目文档**: `README.md`
- **Phaser 官方文档**: https://phaser.io/docs
- **提交问题**: https://github.com/acheng-byte/happycapy-office-ui/issues

---

**Happy Designing! 🎨**
