# HappyCapy Office UI - 设计指南

## 🎨 如何自己设计 UI

这个指南教你如何完全自定义办公室 UI 的外观。

---

## 📐 UI 结构

```
UI 组成部分：
┌─────────────────────────────────────┐
│  背景图片 (800x600)                  │
│  ├── 办公区域                        │
│  ├── 家具和装饰                      │
│  └── 窗户、墙壁等                    │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│  角色精灵 (48x48)                    │
│  └── 水豚（或其他动物/角色）         │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│  互动区域坐标                        │
│  ├── 工作桌 (x, y)                   │
│  ├── 休息区 (x, y)                   │
│  ├── 会议区 (x, y)                   │
│  └── 其他区域...                     │
└─────────────────────────────────────┘
```

---

## 方法 1: 用 Python 代码设计背景（推荐）

### 步骤 1: 修改背景设计脚本

编辑 `create_isometric_office.py`：

```python
# 修改尺寸
width, height = 800, 600  # 可以改成任何尺寸

# 修改颜色方案
floor_base = '#3a3a52'      # 地板颜色
wall_color = '#252538'      # 墙壁颜色
desk_wood = '#8B6F47'       # 桌子颜色
tech_blue = '#4a9eff'       # 科技设备颜色
plant_green = '#4a9e5f'     # 植物颜色
chair_red = '#c44569'       # 椅子颜色

# 添加你自己的家具
def draw_custom_furniture(x, y):
    # 绘制你的自定义家具
    draw_iso_box(x, y, 80, 40, 50, '#your_color', '#your_color2')
```

### 步骤 2: 运行生成脚本

```bash
python create_isometric_office.py
```

生成的图片会保存到 `frontend/office_bg.png`

---

## 方法 2: 使用图像编辑软件设计

### 使用 Photoshop / GIMP / Krita

1. **创建新画布**: 800x600 像素
2. **设计布局**:
   - 绘制地板（建议使用瓷砖图案）
   - 添加墙壁和窗户
   - 放置家具（桌子、椅子、沙发等）
   - 添加装饰品（植物、书架、时钟等）
3. **导出**: 保存为 PNG 格式
4. **替换**: 将文件保存为 `frontend/office_bg.png`

### 推荐的设计工具

- **Aseprite** - 像素艺术专业工具（$19.99）
- **LibreSprite** - Aseprite 的免费开源版本
- **Piskel** - 在线免费像素编辑器 (https://www.piskelapp.com/)
- **Pixel Studio** - 移动端像素艺术 App

---

## 方法 3: 使用 AI 生成背景

### 使用项目集成的 AI 图像生成技能

```bash
# 调用 AI 图像生成技能
/ai-image-generation
```

**提示词示例**：
```
生成一个 800x600 像素的等距像素风格办公室场景，
包含：豪华办公桌、电脑、书架、植物、窗户、地毯，
风格：像素艺术、等距投影、现代办公室、暖色调
```

---

## 🦫 自定义角色

### 修改现有水豚角色

编辑 `create_fullbody_capybara.py`：

```python
# 修改颜色
body_brown = (160, 130, 109)      # 身体颜色
dark_brown = (139, 111, 71)       # 深色部分
light_brown = (180, 150, 120)     # 浅色部分

# 修改尺寸
img = Image.new('RGBA', (48, 48), color=(0, 0, 0, 0))  # 改变尺寸

# 修改身体形状
draw.ellipse([10, 18, 38, 40], fill=body_brown)  # 调整椭圆坐标
```

### 创建全新角色

1. **使用像素编辑器**: Aseprite、Piskel 等
2. **尺寸**: 48x48 像素（或你想要的尺寸）
3. **透明背景**: 保存为 PNG，背景透明
4. **命名**:
   - `capybara_open.png` - 睁眼状态
   - `capybara_closed.png` - 闭眼状态

### 使用其他动物/角色

**示例角色想法**：
- 🐱 猫咪办公助手
- 🐶 狗狗秘书
- 🐧 企鹅程序员
- 🦊 狐狸设计师
- 🐻 熊猫经理
- 🤖 机器人助手

---

## 📍 配置互动区域

编辑 `frontend/index.html`，修改区域坐标：

```javascript
// 在 create() 函数中找到这段代码
areas = {
    workdesk: { x: 285, y: 350 },    // 工作桌区域
    breakroom: { x: 705, y: 495 },   // 休息区
    meeting: { x: 450, y: 220 },     // 会议区
    server: { x: 720, y: 230 },      // 服务器区
    alert: { x: 620, y: 490 }        // 警报区
};
```

### 如何确定坐标？

**方法 1: 使用浏览器开发者工具**

1. 打开 UI 页面
2. 按 F12 打开开发者工具
3. 在 Console 中输入：
```javascript
this.input.on('pointerdown', (pointer) => {
    console.log(`x: ${pointer.x}, y: ${pointer.y}`);
});
```
4. 点击你想要的位置，查看坐标

**方法 2: 使用图像编辑器**

1. 在 Photoshop/GIMP 中打开 `office_bg.png`
2. 鼠标悬停在你想要的位置
3. 查看左下角显示的坐标 (x, y)
4. 将坐标填入 `areas` 对象

---

## 🎨 设计工具和资源

### 在线设计工具

1. **Figma** (https://figma.com)
   - 免费，协作设计
   - 适合布局设计

2. **Piskel** (https://piskelapp.com)
   - 免费在线像素编辑器
   - 支持动画帧

3. **Canva** (https://canva.com)
   - 简单易用
   - 大量模板

### 像素艺术资源

1. **OpenGameArt** (https://opengameart.org)
   - 免费游戏素材
   - 大量像素艺术

2. **itch.io Assets** (https://itch.io/game-assets/free)
   - 免费和付费素材
   - 高质量资源

3. **Kenney** (https://kenney.nl)
   - 大量免费游戏素材
   - 包含办公室相关图标

### 调色板工具

1. **Lospec Palette List** (https://lospec.com/palette-list)
   - 像素艺术调色板
   - 适合复古风格

2. **Coolors** (https://coolors.co)
   - 快速生成配色方案

3. **Adobe Color** (https://color.adobe.com)
   - 专业配色工具

---

## 🛠️ 快速设计工具

### 工具 1: 可视化网页设计工具 ⭐ 推荐

打开 `design_tool.html` 在浏览器中使用：

```bash
# 方法1: 直接打开
open design_tool.html

# 方法2: 使用 Python HTTP 服务器
cd happycapy-office-ui
python -m http.server 8080
# 然后访问: http://localhost:8080/design_tool.html
```

**功能特点：**
- ✅ 可视化画布（800×600）
- ✅ 点击设置互动区域坐标
- ✅ 拖拽加载背景图片
- ✅ 颜色选择器
- ✅ 一键导出配置代码
- ✅ 实时预览标记

**使用流程：**
1. 点击左侧选择颜色方案
2. 在右侧选择要设置的区域（工作桌、休息区等）
3. 在中间画布上点击设置位置
4. 点击"导出配置"获取代码
5. 复制代码到对应文件

---

### 工具 2: Python 命令行设计工具

运行交互式设计脚本：

```bash
python design_office.py
```

**提供 4 种预设风格：**

1. **现代简约风** - 浅色系、极简设计
2. **科技未来风** - 暗色系、霓虹配色
3. **温馨舒适风** - 暖色系、友好氛围
4. **豪华商务风** - 专业大气（默认）
5. **自定义颜色** - 完全自由配置

**示例输出：**
```
🎨 HappyCapy Office UI - 办公室设计工具
============================================================
请选择设计风格：
1. 现代简约风 (Modern Minimalist)
2. 科技未来风 (Tech Futuristic)
3. 温馨舒适风 (Cozy Comfortable)
4. 豪华商务风 (Luxury Business)
5. 自定义颜色 (Custom Colors)
0. 退出
============================================================
请选择 (0-5): 2

🎨 正在生成 科技未来风...
  ✓ 绘制地板...
  ✓ 添加窗户...
  ✓ 摆放家具...
  ✓ 添加装饰...

✅ 完成！
📁 保存位置: frontend/office_bg.png
🎨 风格: 科技未来风
📏 尺寸: 800 × 600 像素
```

---

## 🎯 快速开始示例

### 示例 1: 更改配色方案（5 分钟）

```bash
# 1. 运行设计工具
python design_office.py

# 2. 选择风格（输入 2 选择科技未来风）
请选择 (0-5): 2

# 3. 生成完成，查看效果
python backend/app.py
# 访问 http://localhost:18791
```

### 示例 2: 自定义区域位置（10 分钟）

```bash
# 1. 打开可视化工具
open design_tool.html

# 2. 点击右侧"💼 工作桌区"
# 3. 在画布上点击你想要的位置
# 4. 重复设置其他区域
# 5. 点击"导出配置"
# 6. 复制 JavaScript 代码到 frontend/index.html
```

### 示例 3: 完全自定义设计（30 分钟）

```bash
# 1. 使用 Piskel 在线工具创建背景
# 访问: https://www.piskelapp.com/

# 2. 创建 800×600 画布，设计你的办公室

# 3. 导出为 PNG，保存为 office_bg.png

# 4. 替换文件
cp your_design.png frontend/office_bg.png

# 5. 使用 design_tool.html 设置区域坐标

# 6. 测试效果
python backend/app.py
```

---

## 📚 设计灵感和参考

### 像素艺术办公室参考

- **Stardew Valley** - 温馨舒适的办公室风格
- **Pixel Agents** - 现代等距办公场景
- **Habbo Hotel** - 等距社交空间设计
- **The Sims** - 家具和装饰布局

### 配色方案参考

1. **专业商务**
   ```
   地板: #3a3a52  墙壁: #252538
   桌子: #8B6F47  科技: #4a9eff
   ```

2. **清新自然**
   ```
   地板: #e8f5e9  墙壁: #ffffff
   桌子: #8d6e63  科技: #42a5f5
   ```

3. **暗黑科技**
   ```
   地板: #1a1a2e  墙壁: #0f3460
   桌子: #533483  科技: #00d4ff
   ```

4. **温暖阳光**
   ```
   地板: #ffeaa7  墙壁: #fab1a0
   桌子: #e17055  科技: #74b9ff
   ```

---

## 🔧 高级自定义

### 修改角色尺寸

编辑 `create_fullbody_capybara.py`：

```python
# 改变角色大小
img = Image.new('RGBA', (64, 64), color=(0, 0, 0, 0))  # 从 48x48 改为 64x64

# 相应调整身体部位坐标
draw.ellipse([15, 25, 49, 52], fill=body_brown)  # 放大身体
```

### 添加新的互动区域

编辑 `frontend/index.html`：

```javascript
// 添加新区域
areas = {
    workdesk: { x: 285, y: 350 },
    breakroom: { x: 705, y: 495 },
    meeting: { x: 450, y: 220 },
    server: { x: 720, y: 230 },
    kitchen: { x: 100, y: 500 },  // 新增：厨房区
    library: { x: 600, y: 300 }   // 新增：图书区
};

// 添加新状态映射
function moveCapybaraToArea(state) {
    if (state === 'cooking') return areas.kitchen;
    if (state === 'reading') return areas.library;
    // ... 其他状态
}
```

### 添加动画效果

在 `frontend/index.html` 中添加：

```javascript
// 添加眨眼动画
this.time.addEvent({
    delay: 3000,
    callback: () => {
        capybara.setTexture('capy_closed');
        this.time.delayedCall(200, () => {
            capybara.setTexture('capy_open');
        });
    },
    loop: true
});

// 添加呼吸效果
this.tweens.add({
    targets: capybara,
    scaleY: 1.05,
    duration: 1000,
    yoyo: true,
    repeat: -1
});
```

---

## 💡 设计技巧

### 1. 保持简洁
- 不要在 800×600 的空间里塞太多元素
- 留白也是设计的一部分

### 2. 颜色协调
- 使用 3-5 种主色
- 相邻元素避免颜色相近

### 3. 层次分明
- 背景（地板、墙壁）→ 暗色
- 家具（桌子、椅子）→ 中色
- 装饰（植物、灯光）→ 亮色

### 4. 功能区域清晰
- 工作区聚集在一起
- 休息区单独分开
- 通道留足空间

### 5. 像素完美
- 使用偶数尺寸（48×48, 64×64）
- 避免奇数像素导致的模糊

---

## 🆘 常见问题

**Q: 如何让背景图更清晰？**
A: 确保图片是 800×600 像素，不要放大或缩小。使用像素艺术工具创建。

**Q: 角色显示不正确怎么办？**
A: 检查 `capybara_open.png` 和 `capybara_closed.png` 是否存在，是否为透明背景。

**Q: 区域坐标怎么调试？**
A: 使用 `design_tool.html` 可视化工具，或者在浏览器控制台输出坐标。

**Q: 可以改变画布尺寸吗？**
A: 可以，但需要同时修改：
- Python 脚本中的 `width, height`
- HTML 中的 canvas 尺寸
- CSS 中的相关尺寸

**Q: 如何添加更多动画？**
A: 使用 Phaser 的 Tween 系统，参考官方文档：https://photonstorm.github.io/phaser3-docs/

---

## 📦 导出和分享

### 生成完整包

```bash
# 1. 确保所有文件都在
ls frontend/office_bg.png
ls frontend/capybara_open.png
ls frontend/capybara_closed.png

# 2. 测试运行
python backend/app.py

# 3. 打包
zip -r happycapy-office-custom.zip \
    backend/ \
    frontend/ \
    *.py \
    *.md \
    README.md
```

### 分享到 GitHub

```bash
# 提交更改
git add .
git commit -m "自定义 UI 设计: [描述你的更改]"
git push origin main
```

---

## 🎓 学习资源

- **Phaser 官方教程**: https://phaser.io/tutorials
- **像素艺术教程**: https://lospec.com/pixel-art-tutorials
- **等距投影指南**: https://medium.com/retronator-magazine/isometric-pixel-art-101
- **配色工具**: https://coolors.co
- **精灵图编辑**: https://www.piskelapp.com

---

**有问题？**
- 查看 [README.md](README.md)
- 提交 Issue: https://github.com/acheng-byte/happycapy-office-ui/issues
- 参考 Phaser 文档: https://phaser.io/docs
